# Cloudflare Pages config — wrangler.toml mínimo para que wrangler detecte este repo
# como Pages (no como Worker) sin hacer auto-detección del workspace.
# Solo el campo `pages_build_output_dir`; `name` lo pone CF Pages en su dashboard.
pages_build_output_dir = "./dist"
